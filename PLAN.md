# PLAN — moved

The development plan has been split by phase to keep token usage manageable for any single task.

**Index:** [`docs/plan/README.md`](./docs/plan/README.md)

Each phase is its own file under `docs/plan/`. Read only the file relevant to your current task.

References elsewhere in the codebase that say `PLAN.md` Phase N now mean `docs/plan/phase-N-*.md`.
