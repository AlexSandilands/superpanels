dist/
node_modules/
package-lock.json

# ts-rs writes these files from Rust definitions in superpanels-{gui,core};
# the generated output doesn't match Prettier's formatting and will be
# overwritten on every test run. Source of truth is the Rust types.
# `profile-helpers.ts` is the only hand-written sibling — keep it formatted.
src/lib/types/*.ts
!src/lib/types/profile-helpers.ts
