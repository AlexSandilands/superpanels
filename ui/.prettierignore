dist/
node_modules/
package-lock.json

# ts-rs writes these files from Rust definitions in superpanels-gui;
# the generated output doesn't match Prettier's formatting and will be
# overwritten on every build. Source of truth is the Rust types.
# Hand-written siblings in this directory (e.g. `profile.ts`) are NOT
# ignored — list generated files explicitly so the ignore stays narrow.
src/lib/types/IpcError.ts
src/lib/types/LibraryFilter.ts
src/lib/types/PreviewArgs.ts
